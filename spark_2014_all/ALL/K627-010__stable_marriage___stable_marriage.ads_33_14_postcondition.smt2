;; produced by cvc4_16.drv ;;
(set-info :smt-lib-version 2.6)
(set-logic AUFBVFPDTNIRA)
(set-info :source |VC generated by SPARK 2014|)
(set-info :category industrial)
(set-info :status unknown)
;;; generated by SMT-LIB2 driver
;;; SMT-LIB2 driver: bit-vectors, common part
;;; SMT-LIB2: integer arithmetic
(declare-datatypes ((tuple0 0)) (((Tuple0))))
(declare-sort us_private 0)

(declare-fun private__bool_eq (us_private us_private) Bool)

(declare-const us_null_ext__ us_private)

(declare-sort us_type_of_heap 0)

(declare-datatypes ((us_type_of_heap__ref 0))
(((mk___type_of_heap__ref (us_type_of_heap__content us_type_of_heap)))))
(declare-sort us_image 0)

(declare-datatypes ((int__ref 0)) (((mk_int__ref (int__content Int)))))
(declare-datatypes ((bool__ref 0)) (((mk_bool__ref (bool__content Bool)))))
(declare-datatypes ((real__ref 0)) (((mk_real__ref (real__content Real)))))
(declare-datatypes ((us_private__ref 0))
(((mk___private__ref (us_private__content us_private)))))
(define-fun int__ref___projection ((a int__ref)) Int (int__content a))

(define-fun bool__ref___projection ((a bool__ref)) Bool (bool__content a))

(define-fun real__ref___projection ((a real__ref)) Real (real__content a))

(define-fun us_private__ref___projection ((a us_private__ref)) us_private
  (us_private__content a))

(define-fun to_int1 ((b Bool)) Int (ite (= b true) 1 0))

(define-fun of_int ((i Int)) Bool (ite (= i 0) false true))

(define-fun in_range ((x Int)) Bool (or (= x 0) (= x 1)))

(declare-fun attr__ATTRIBUTE_IMAGE (Bool) us_image)

(declare-fun attr__ATTRIBUTE_VALUE__pre_check (us_image) Bool)

(declare-fun attr__ATTRIBUTE_VALUE (us_image) Bool)

(declare-sort group2_id 0)

(define-fun in_range1 ((x Int)) Bool (and (<= 1 x) (<= x 64)))

(declare-fun attr__ATTRIBUTE_IMAGE1 (Int) us_image)

(declare-fun attr__ATTRIBUTE_VALUE__pre_check1 (us_image) Bool)

(declare-fun attr__ATTRIBUTE_VALUE1 (us_image) Int)

(declare-fun user_eq (group2_id group2_id) Bool)

(declare-const dummy group2_id)

(declare-datatypes ((group2_id__ref 0))
(((mk_group2_id__ref (group2_id__content group2_id)))))
(define-fun group2_id__ref___projection ((a group2_id__ref)) group2_id
  (group2_id__content a))

(declare-fun to_rep (group2_id) Int)

(declare-fun of_rep (Int) group2_id)

;; inversion_axiom
  (assert
  (forall ((x group2_id))
  (! (= (of_rep (to_rep x)) x) :pattern ((to_rep x)) )))

;; range_axiom
  (assert
  (forall ((x group2_id)) (! (in_range1 (to_rep x)) :pattern ((to_rep x)) )))

;; coerce_axiom
  (assert
  (forall ((x Int))
  (! (=> (in_range1 x) (= (to_rep (of_rep x)) x)) :pattern ((to_rep
                                                            (of_rep x))) )))

(declare-sort ranking 0)

(define-fun in_range2 ((x Int)) Bool (and (<= 1 x) (<= x 64)))

(declare-fun attr__ATTRIBUTE_IMAGE2 (Int) us_image)

(declare-fun attr__ATTRIBUTE_VALUE__pre_check2 (us_image) Bool)

(declare-fun attr__ATTRIBUTE_VALUE2 (us_image) Int)

(declare-fun user_eq1 (ranking ranking) Bool)

(declare-const dummy1 ranking)

(declare-datatypes ((ranking__ref 0))
(((mk_ranking__ref (ranking__content ranking)))))
(define-fun ranking__ref___projection ((a ranking__ref)) ranking (ranking__content
                                                                 a))

(declare-datatypes ((map__ref 0))
(((mk_map__ref (map__content (Array Int group2_id))))))
(declare-fun slide ((Array Int group2_id) Int Int) (Array Int group2_id))

;; slide_eq
  (assert
  (forall ((a (Array Int group2_id)))
  (forall ((first Int))
  (! (= (slide a first first) a) :pattern ((slide a first first)) ))))

;; slide_def
  (assert
  (forall ((a (Array Int group2_id)))
  (forall ((old_first Int))
  (forall ((new_first Int))
  (forall ((i Int))
  (! (= (select (slide a old_first new_first) i) (select a (- i (- new_first old_first)))) :pattern ((select
  (slide a old_first new_first) i)) ))))))

(declare-fun concat1 ((Array Int group2_id) Int Int (Array Int group2_id) Int
  Int) (Array Int group2_id))

;; concat_def
  (assert
  (forall ((a (Array Int group2_id)) (b (Array Int group2_id)))
  (forall ((a_first Int) (a_last Int) (b_first Int) (b_last Int))
  (forall ((i Int))
  (! (and
     (=> (and (<= a_first i) (<= i a_last))
     (= (select (concat1 a a_first a_last b b_first b_last) i) (select a i)))
     (=> (< a_last i)
     (= (select (concat1 a a_first a_last b b_first b_last) i) (select b (+ (- i a_last) (- b_first 1)))))) :pattern ((select
  (concat1 a a_first a_last b b_first b_last) i)) )))))

(declare-fun singleton1 (group2_id Int) (Array Int group2_id))

;; singleton_def
  (assert
  (forall ((v group2_id))
  (forall ((i Int))
  (! (= (select (singleton1 v i) i) v) :pattern ((select (singleton1 v i) i)) ))))

(define-fun bool_eq ((a (Array Int group2_id)) (a__first Int) (a__last Int)
  (b (Array Int group2_id)) (b__first Int)
  (b__last Int)) Bool (ite (and
                           (ite (<= a__first a__last)
                           (and (<= b__first b__last)
                           (= (- a__last a__first) (- b__last b__first)))
                           (< b__last b__first))
                           (forall ((temp___idx_133 Int))
                           (=>
                           (and (<= a__first temp___idx_133)
                           (<= temp___idx_133 a__last))
                           (= (to_rep (select a temp___idx_133)) (to_rep
                                                                 (select b (+ (- b__first a__first) temp___idx_133)))))))
                      true false))

;; bool_eq_rev
  (assert
  (forall ((a (Array Int group2_id)) (b (Array Int group2_id)))
  (forall ((a__first Int) (a__last Int) (b__first Int) (b__last Int))
  (=> (= (bool_eq b b__first b__last a a__first a__last) true)
  (and
  (ite (<= a__first a__last)
  (and (<= b__first b__last) (= (- a__last a__first) (- b__last b__first)))
  (< b__last b__first))
  (forall ((temp___idx_133 Int))
  (=> (and (<= a__first temp___idx_133) (<= temp___idx_133 a__last))
  (= (to_rep (select a temp___idx_133)) (to_rep
                                        (select b (+ (- b__first a__first) temp___idx_133)))))))))))

(declare-fun compare ((Array Int group2_id) Int Int (Array Int group2_id) Int
  Int) Int)

;; compare_def_eq
  (assert
  (forall ((a (Array Int group2_id)) (b (Array Int group2_id)))
  (forall ((a_first Int) (a_last Int) (b_first Int) (b_last Int))
  (! (= (= (compare a a_first a_last b b_first b_last) 0)
     (= (bool_eq a a_first a_last b b_first b_last) true)) :pattern (
  (compare a a_first a_last b b_first b_last)) ))))

;; compare_def_lt
  (assert
  (forall ((a (Array Int group2_id)) (b (Array Int group2_id)))
  (forall ((a_first Int) (a_last Int) (b_first Int) (b_last Int))
  (! (= (< (compare a a_first a_last b b_first b_last) 0)
     (exists ((i Int) (j Int))
     (and (<= i a_last)
     (and (< j b_last)
     (and (= (bool_eq a a_first i b b_first j) true)
     (or (= i a_last)
     (and (< i a_last)
     (< (to_rep (select a (+ i 1))) (to_rep (select b (+ j 1))))))))))) :pattern (
  (compare a a_first a_last b b_first b_last)) ))))

;; compare_def_gt
  (assert
  (forall ((a (Array Int group2_id)) (b (Array Int group2_id)))
  (forall ((a_first Int) (a_last Int) (b_first Int) (b_last Int))
  (! (= (< 0 (compare a a_first a_last b b_first b_last))
     (exists ((i Int) (j Int))
     (and (<= i b_last)
     (and (< j a_last)
     (and (= (bool_eq a a_first j b b_first i) true)
     (or (= i b_last)
     (and (< i b_last)
     (< (to_rep (select b (+ i 1))) (to_rep (select a (+ j 1))))))))))) :pattern (
  (compare a a_first a_last b b_first b_last)) ))))

(declare-const r2 (Array Int group2_id))

(declare-const attr__ATTRIBUTE_ADDRESS Int)

(declare-datatypes ((map__ref1 0))
(((mk_map__ref1 (map__content1 (Array Int Bool))))))
(declare-fun slide1 ((Array Int Bool) Int Int) (Array Int Bool))

;; slide_eq
  (assert
  (forall ((a (Array Int Bool)))
  (forall ((first Int))
  (! (= (slide1 a first first) a) :pattern ((slide1 a first first)) ))))

;; slide_def
  (assert
  (forall ((a (Array Int Bool)))
  (forall ((old_first Int))
  (forall ((new_first Int))
  (forall ((i Int))
  (! (= (select (slide1 a old_first new_first) i) (select a (- i (- new_first old_first)))) :pattern ((select
  (slide1 a old_first new_first) i)) ))))))

(declare-fun concat2 ((Array Int Bool) Int Int (Array Int Bool) Int
  Int) (Array Int Bool))

;; concat_def
  (assert
  (forall ((a (Array Int Bool)) (b (Array Int Bool)))
  (forall ((a_first Int) (a_last Int) (b_first Int) (b_last Int))
  (forall ((i Int))
  (! (and
     (=> (and (<= a_first i) (<= i a_last))
     (= (select (concat2 a a_first a_last b b_first b_last) i) (select a i)))
     (=> (< a_last i)
     (= (select (concat2 a a_first a_last b b_first b_last) i) (select b (+ (- i a_last) (- b_first 1)))))) :pattern ((select
  (concat2 a a_first a_last b b_first b_last) i)) )))))

(declare-fun singleton2 (Bool Int) (Array Int Bool))

;; singleton_def
  (assert
  (forall ((v Bool))
  (forall ((i Int))
  (! (= (select (singleton2 v i) i) v) :pattern ((select (singleton2 v i) i)) ))))

(define-fun bool_eq1 ((a (Array Int Bool)) (a__first Int) (a__last Int)
  (b (Array Int Bool)) (b__first Int)
  (b__last Int)) Bool (ite (and
                           (ite (<= a__first a__last)
                           (and (<= b__first b__last)
                           (= (- a__last a__first) (- b__last b__first)))
                           (< b__last b__first))
                           (forall ((temp___idx_137 Int))
                           (=>
                           (and (<= a__first temp___idx_137)
                           (<= temp___idx_137 a__last))
                           (= (select a temp___idx_137) (select b (+ (- b__first a__first) temp___idx_137))))))
                      true false))

;; bool_eq_rev
  (assert
  (forall ((a (Array Int Bool)) (b (Array Int Bool)))
  (forall ((a__first Int) (a__last Int) (b__first Int) (b__last Int))
  (=> (= (bool_eq1 b b__first b__last a a__first a__last) true)
  (and
  (ite (<= a__first a__last)
  (and (<= b__first b__last) (= (- a__last a__first) (- b__last b__first)))
  (< b__last b__first))
  (forall ((temp___idx_137 Int))
  (=> (and (<= a__first temp___idx_137) (<= temp___idx_137 a__last))
  (= (select a temp___idx_137) (select b (+ (- b__first a__first) temp___idx_137))))))))))

(declare-fun compare1 ((Array Int Bool) Int Int (Array Int Bool) Int
  Int) Int)

;; compare_def_eq
  (assert
  (forall ((a (Array Int Bool)) (b (Array Int Bool)))
  (forall ((a_first Int) (a_last Int) (b_first Int) (b_last Int))
  (! (= (= (compare1 a a_first a_last b b_first b_last) 0)
     (= (bool_eq1 a a_first a_last b b_first b_last) true)) :pattern (
  (compare1 a a_first a_last b b_first b_last)) ))))

;; compare_def_lt
  (assert
  (forall ((a (Array Int Bool)) (b (Array Int Bool)))
  (forall ((a_first Int) (a_last Int) (b_first Int) (b_last Int))
  (! (= (< (compare1 a a_first a_last b b_first b_last) 0)
     (exists ((i Int) (j Int))
     (and (<= i a_last)
     (and (< j b_last)
     (and (= (bool_eq1 a a_first i b b_first j) true)
     (or (= i a_last)
     (and (< i a_last)
     (< (to_int1 (select a (+ i 1))) (to_int1 (select b (+ j 1))))))))))) :pattern (
  (compare1 a a_first a_last b b_first b_last)) ))))

;; compare_def_gt
  (assert
  (forall ((a (Array Int Bool)) (b (Array Int Bool)))
  (forall ((a_first Int) (a_last Int) (b_first Int) (b_last Int))
  (! (= (< 0 (compare1 a a_first a_last b b_first b_last))
     (exists ((i Int) (j Int))
     (and (<= i b_last)
     (and (< j a_last)
     (and (= (bool_eq1 a a_first j b b_first i) true)
     (or (= i b_last)
     (and (< i b_last)
     (< (to_int1 (select b (+ i 1))) (to_int1 (select a (+ j 1))))))))))) :pattern (
  (compare1 a a_first a_last b b_first b_last)) ))))

(declare-fun xorb ((Array Int Bool) Int Int (Array Int Bool) Int
  Int) (Array Int Bool))

(declare-fun andb ((Array Int Bool) Int Int (Array Int Bool) Int
  Int) (Array Int Bool))

(declare-fun orb ((Array Int Bool) Int Int (Array Int Bool) Int
  Int) (Array Int Bool))

(declare-fun notb ((Array Int Bool) Int Int) (Array Int Bool))

;; op_def
  (assert
  (forall ((a (Array Int Bool)) (b (Array Int Bool)))
  (forall ((a_first Int) (a_last Int) (b_first Int) (b_last Int))
  (forall ((i Int))
  (! (=> (and (<= a_first i) (<= i a_last))
     (= (select (orb a a_first a_last b b_first b_last) i) (or (select a i) (select b (+ (- i a_first) b_first))))) :pattern ((select
  (orb a a_first a_last b b_first b_last) i)) )))))

;; op_def
  (assert
  (forall ((a (Array Int Bool)) (b (Array Int Bool)))
  (forall ((a_first Int) (a_last Int) (b_first Int) (b_last Int))
  (forall ((i Int))
  (! (=> (and (<= a_first i) (<= i a_last))
     (= (select (andb a a_first a_last b b_first b_last) i) (and (select a i) (select b (+ (- i a_first) b_first))))) :pattern ((select
  (andb a a_first a_last b b_first b_last) i)) )))))

;; op_def
  (assert
  (forall ((a (Array Int Bool)) (b (Array Int Bool)))
  (forall ((a_first Int) (a_last Int) (b_first Int) (b_last Int))
  (forall ((i Int))
  (! (=> (and (<= a_first i) (<= i a_last))
     (= (select (xorb a a_first a_last b b_first b_last) i) (xor (select a i) (select b (+ (- i a_first) b_first))))) :pattern ((select
  (xorb a a_first a_last b b_first b_last) i)) )))))

;; notb_def
  (assert
  (forall ((a (Array Int Bool)))
  (forall ((a_first Int) (a_last Int))
  (forall ((i Int))
  (! (=> (and (<= a_first i) (<= i a_last))
     (= (select (notb a a_first a_last) i) (not (select a i)))) :pattern ((select
  (notb a a_first a_last) i)) )))))

(declare-const attr__ATTRIBUTE_ADDRESS1 Int)

(declare-const attr__ATTRIBUTE_ADDRESS2 Int)

(declare-fun stable_marriage__is_permutation_2__seen__aggregate_def (Bool) (Array Int Bool))

;; def_axiom
  (assert
  (forall ((temp___420 Bool))
  (forall ((temp___421 Int))
  (= (select (stable_marriage__is_permutation_2__seen__aggregate_def
             temp___420) temp___421) temp___420))))

(declare-fun temp___427 (Bool) (Array Int Bool))

;; def_axiom
  (assert
  (forall ((temp___429 Bool))
  (forall ((temp___430 Int))
  (= (select (temp___427 temp___429) temp___430) temp___429))))

(declare-fun temp___433 (Bool) (Array Int Bool))

;; def_axiom
  (assert
  (forall ((temp___435 Bool))
  (forall ((temp___436 Int))
  (= (select (temp___433 temp___435) temp___436) temp___435))))

(define-fun dynamic_invariant ((temp___expr_149 Int)
  (temp___is_init_146 Bool) (temp___skip_constant_147 Bool)
  (temp___do_toplevel_148 Bool)) Bool (=>
                                      (or (= temp___is_init_146 true)
                                      (<= 1 64)) (in_range1 temp___expr_149)))

(define-fun dynamic_invariant1 ((temp___expr_155 Int)
  (temp___is_init_152 Bool) (temp___skip_constant_153 Bool)
  (temp___do_toplevel_154 Bool)) Bool (=>
                                      (or (= temp___is_init_152 true)
                                      (<= 1 64)) (in_range2 temp___expr_155)))

(declare-const seen (Array Int Bool))

(declare-const rank Int)

(declare-const stable_marriage__is_permutation_2__result Bool)

(declare-const temp___423 (Array Int Bool))

(declare-const temp___422 Int)

(declare-const result (Array Int Bool))

(declare-const seen1 (Array Int Bool))

(declare-const result1 Int)

(declare-const rank1 Int)

(declare-const seen2 (Array Int Bool))

(declare-const rank2 Int)

(declare-const result2 (Array Int Bool))

(declare-const seen3 (Array Int Bool))

(declare-const seen4 (Array Int Bool))

(declare-const rank3 Int)

(declare-const seen5 (Array Int Bool))

(declare-const rank4 Int)

(declare-const seen6 (Array Int Bool))

(declare-const rank5 Int)

(declare-const seen7 (Array Int Bool))

(declare-const rank6 Int)

(declare-const result3 Bool)

(declare-const stable_marriage__is_permutation_2__result1 Bool)

(declare-const stable_marriage__is_permutation_2__result2 Bool)

(declare-const stable_marriage__is_permutation_2__result3 Bool)

(declare-const seen8 (Array Int Bool))

(declare-const rank7 Int)

(declare-const stable_marriage__is_permutation_2__result4 Bool)

(declare-const seen9 (Array Int Bool))

(declare-const rank8 Int)

(declare-const stable_marriage__is_permutation_2__result5 Bool)

(define-fun rank9 () int__ref (mk_int__ref rank5))

(define-fun seen10 () map__ref1 (mk_map__ref1 seen6))

(declare-const result4 Bool)

;; H
  (assert (= (mk_map__ref1 result) (mk_map__ref1 seen)))

;; H
  (assert
  (= seen1 (stable_marriage__is_permutation_2__seen__aggregate_def
           (of_int 0))))

;; H
  (assert (= (mk_int__ref result1) (mk_int__ref rank)))

;; H
  (assert (= rank1 1))

;; H
  (assert (=> (and (<= 1 rank1) (<= rank1 64)) (= temp___423 seen1)))

;; H
  (assert (=> (and (<= 1 rank1) (<= rank1 64)) (= temp___422 rank1)))

;; H
  (assert
  (=> (and (<= 1 rank1) (<= rank1 64))
  (forall ((g2 Int))
  (=> (and (<= 1 g2) (<= g2 64))
  (= (= (select seen2 g2) true)
  (exists ((prev Int))
  (and (and (<= 1 prev) (<= prev (- rank2 1)))
  (= (to_rep (select r2 prev)) g2))))))))

;; H
  (assert
  (=> (and (<= 1 rank1) (<= rank1 64))
  (and (=> (<= 1 64) (in_range2 rank2)) (and (<= 1 rank2) (<= rank2 64)))))

;; H
  (assert (=> (and (<= 1 rank1) (<= rank1 64)) (= seen2 result2)))

;; H
  (assert
  (=> (and (<= 1 rank1) (<= rank1 64))
  (= seen3 (store seen2 (to_rep (select r2 rank2)) (of_int 1)))))

;; H
  (assert (=> (and (<= 1 rank1) (<= rank1 64)) (= rank2 64)))

;; H
  (assert (=> (and (<= 1 rank1) (<= rank1 64)) (= rank2 rank3)))

;; H
  (assert (=> (and (<= 1 rank1) (<= rank1 64)) (= seen3 seen4)))

;; H
  (assert (=> (and (<= 1 rank1) (<= rank1 64)) (= rank4 rank2)))

;; H
  (assert (=> (and (<= 1 rank1) (<= rank1 64)) (= seen5 seen3)))

;; H
  (assert
  (=> (and (<= 1 rank1) (<= rank1 64)) (= rank9 (mk_int__ref rank3))))

;; H
  (assert
  (=> (and (<= 1 rank1) (<= rank1 64)) (= seen10 (mk_map__ref1 seen4))))

;; H
  (assert (=> (and (<= 1 rank1) (<= rank1 64)) (= rank6 rank4)))

;; H
  (assert (=> (and (<= 1 rank1) (<= rank1 64)) (= seen7 seen5)))

;; H
  (assert (=> (not (and (<= 1 rank1) (<= rank1 64))) (= rank1 rank5)))

;; H
  (assert (=> (not (and (<= 1 rank1) (<= rank1 64))) (= seen1 seen6)))

;; H
  (assert (=> (not (and (<= 1 rank1) (<= rank1 64))) (= rank6 rank1)))

;; H
  (assert (=> (not (and (<= 1 rank1) (<= rank1 64))) (= seen7 seen1)))

;; H
  (assert
  (= (= (bool_eq1 seen6 1 64 (temp___427 (of_int 1)) 1 64) true)
  (forall ((g2 Int))
  (=> (and (<= 1 g2) (<= g2 64))
  (and (= (select seen6 g2) true)
  (exists ((rank10 Int))
  (and (and (<= 1 rank10) (<= rank10 64)) (= (to_rep (select r2 rank10)) g2))))))))

;; H
  (assert
  (= stable_marriage__is_permutation_2__result1 stable_marriage__is_permutation_2__result2))

;; H
  (assert
  (= stable_marriage__is_permutation_2__result3 stable_marriage__is_permutation_2__result1))

;; H
  (assert (= result3 stable_marriage__is_permutation_2__result))

;; H
  (assert
  (= stable_marriage__is_permutation_2__result1 (bool_eq1 seen6 1 64
                                                (temp___433 (of_int 1)) 1 64)))

;; H
  (assert
  (= (mk_bool__ref stable_marriage__is_permutation_2__result4) (mk_bool__ref
                                                               stable_marriage__is_permutation_2__result2)))

;; H
  (assert (= (mk_int__ref rank7) rank9))

;; H
  (assert (= (mk_map__ref1 seen8) seen10))

;; H
  (assert
  (= stable_marriage__is_permutation_2__result5 stable_marriage__is_permutation_2__result3))

;; H
  (assert (= rank8 rank6))

;; H
  (assert (= seen9 seen7))

;; H
  (assert (= result4 stable_marriage__is_permutation_2__result4))

;; H
  (assert (= stable_marriage__is_permutation_2__result4 true))

(declare-const g2 Int)

;; H
  (assert (<= 1 g2))

;; H
  (assert (<= g2 64))

(assert
;; WP_parameter_def
 ;; File "stable_marriage.ads", line 32, characters 0-0
  (not
  (exists ((rank10 Int))
  (and (and (<= 1 rank10) (<= rank10 64)) (= (to_rep (select r2 rank10)) g2)))))
(check-sat)
(exit)

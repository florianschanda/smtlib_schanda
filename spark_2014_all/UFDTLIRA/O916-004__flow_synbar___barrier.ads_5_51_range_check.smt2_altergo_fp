(* this is the prelude for Alt-Ergo, version >= 0.95.2 *)
(* this is a prelude for Alt-Ergo integer arithmetic *)
logic match_bool : bool, 'a, 'a -> 'a

axiom match_bool_True :
  (forall z:'a. forall z1:'a. (match_bool(true, z, z1) = z))

axiom match_bool_False :
  (forall z:'a. forall z1:'a. (match_bool(false, z, z1) = z1))

function andb(x: bool, y: bool) : bool = match_bool(x, y, false)

function orb(x: bool, y: bool) : bool = match_bool(x, true, y)

function notb(x: bool) : bool = match_bool(x, false, true)

function xorb(x: bool, y: bool) : bool = match_bool(x, notb(y), y)

function implb(x: bool, y: bool) : bool = match_bool(x, y, true)

type us_private

logic private__bool_eq : us_private, us_private -> bool

logic us_null_ext__ : us_private

type us_type_of_heap

type us_type_of_heap__ref = { us_type_of_heap__content : us_type_of_heap
}

type us_image

type int__ref = { int__content : int }

type bool__ref = { bool__content : bool }

type real__ref = { real__content : real }

type us_private__ref = { us_private__content : us_private
}

function int__ref___projection(a: int__ref) : int = (a).int__content

function bool__ref___projection(a: bool__ref) : bool = (a).bool__content

function real__ref___projection(a: real__ref) : real = (a).real__content

function us_private__ref___projection(a: us_private__ref) : us_private =
  (a).us_private__content

type barrier_limit

predicate in_range(x: int) = ((1 <= x) and (x <= 2147483647))

logic bool_eq : int, int -> bool

logic bool_ne : int, int -> bool

logic bool_lt : int, int -> bool

logic bool_le : int, int -> bool

logic bool_gt : int, int -> bool

logic bool_ge : int, int -> bool

axiom bool_eq_axiom :
  (forall x:int. (forall y:int. ((bool_eq(x, y) = true) <-> (x = y))))

axiom bool_ne_axiom :
  (forall x:int. (forall y:int. ((bool_ne(x, y) = true) <-> (not (x = y)))))

axiom bool_lt_axiom :
  (forall x:int. (forall y:int. ((bool_lt(x, y) = true) <-> (x <  y))))

axiom bool_int__le_axiom :
  (forall x:int. (forall y:int. ((bool_le(x, y) = true) <-> (x <= y))))

axiom bool_gt_axiom :
  (forall x:int. (forall y:int. ((bool_gt(x, y) = true) <-> (y <  x))))

axiom bool_ge_axiom :
  (forall x:int. (forall y:int. ((bool_ge(x, y) = true) <-> (y <= x))))

logic bool_eq1 : int, int -> bool

axiom bool_eq_def :
  (forall x:int. forall y:int [bool_eq1(x, y)]. (((x = y) -> (bool_eq1(x,
  y) = true)) and ((not (x = y)) -> (bool_eq1(x, y) = false))))

logic attr__ATTRIBUTE_IMAGE : int -> us_image

logic attr__ATTRIBUTE_VALUE__pre_check : us_image -> prop

logic attr__ATTRIBUTE_VALUE : us_image -> int

logic user_eq : barrier_limit, barrier_limit -> bool

logic dummy : barrier_limit

type barrier_limit__ref = { barrier_limit__content : barrier_limit
}

function barrier_limit__ref___projection(a: barrier_limit__ref) : barrier_limit =
  (a).barrier_limit__content

logic to_rep : barrier_limit -> int

logic of_rep : int -> barrier_limit

axiom inversion_axiom :
  (forall x:barrier_limit [to_rep(x)]. (of_rep(to_rep(x)) = x))

axiom range_axiom : (forall x:barrier_limit [to_rep(x)]. in_range(to_rep(x)))

axiom coerce_axiom :
  (forall x:int [to_rep(of_rep(x))]. (in_range(x) ->
  (to_rep(of_rep(x)) = x)))

type us_split_discrs = {
rec__synchronous_barriers__synchronous_barrier__release_threshold :
barrier_limit
}

function us_split_discrs___projection(a: us_split_discrs) : barrier_limit =
  (a).rec__synchronous_barriers__synchronous_barrier__release_threshold

type us_split_discrs__ref = { us_split_discrs__content : us_split_discrs
}

function us_split_discrs__ref___projection(a: us_split_discrs__ref) : us_split_discrs =
  (a).us_split_discrs__content

type us_rep = { us_split_discrs1 : us_split_discrs
}

function us_rep___projection(a: us_rep) : us_split_discrs =
  (a).us_split_discrs1

logic value__size : int

logic object__size : us_rep -> int

logic value__alignment : int

logic object__alignment : us_rep -> int

axiom value__size_axiom : (0 <= value__size)

axiom object__size_axiom :
  (forall a:barrier_limit. (0 <= object__size({ us_split_discrs1 = {
  rec__synchronous_barriers__synchronous_barrier__release_threshold = a }
  })))

axiom value__alignment_axiom : (0 <= value__alignment)

axiom object__alignment_axiom :
  (forall a:barrier_limit. (0 <= object__alignment({ us_split_discrs1 = {
  rec__synchronous_barriers__synchronous_barrier__release_threshold = a }
  })))

logic synchronous_barriers__synchronous_barrier__release_threshold__first__bit : int

logic synchronous_barriers__synchronous_barrier__release_threshold__last__bit : int

logic synchronous_barriers__synchronous_barrier__release_threshold__position : int

axiom synchronous_barriers__synchronous_barrier__release_threshold__first__bit_axiom :
  (0 <= synchronous_barriers__synchronous_barrier__release_threshold__first__bit)

axiom synchronous_barriers__synchronous_barrier__release_threshold__last__bit_axiom :
  (synchronous_barriers__synchronous_barrier__release_threshold__first__bit <  synchronous_barriers__synchronous_barrier__release_threshold__last__bit)

axiom synchronous_barriers__synchronous_barrier__release_threshold__position_axiom :
  (0 <= synchronous_barriers__synchronous_barrier__release_threshold__position)

logic user_eq1 : us_rep, us_rep -> bool

type synchronous_barrier__ref = { synchronous_barrier__content : us_rep
}

function synchronous_barrier__ref___projection(a: synchronous_barrier__ref) : us_rep =
  (a).synchronous_barrier__content

type us_rep1 = { us_split_discrs2 : us_split_discrs
}

function us_rep___2__projection(a: us_rep1) : us_split_discrs =
  (a).us_split_discrs2

function to_base(a: us_rep1) : us_rep = { us_split_discrs1 =
  (a).us_split_discrs2 }

function of_base(r: us_rep) : us_rep1 = { us_split_discrs2 =
  (r).us_split_discrs1 }

predicate in_range1(rec__synchronous_barriers__synchronous_barrier__release_threshold1: int,
  a: us_rep) =
  (rec__synchronous_barriers__synchronous_barrier__release_threshold1 = to_rep(((a).us_split_discrs1).rec__synchronous_barriers__synchronous_barrier__release_threshold))

logic value__size1 : int

logic object__size1 : us_rep1 -> int

logic value__alignment1 : int

logic object__alignment1 : us_rep1 -> int

axiom value__size_axiom1 : (0 <= value__size1)

axiom object__size_axiom1 :
  (forall a:barrier_limit. (0 <= object__size1({ us_split_discrs2 = {
  rec__synchronous_barriers__synchronous_barrier__release_threshold = a }
  })))

axiom value__alignment_axiom1 : (0 <= value__alignment1)

axiom object__alignment_axiom1 :
  (forall a:barrier_limit. (0 <= object__alignment1({ us_split_discrs2 = {
  rec__synchronous_barriers__synchronous_barrier__release_threshold = a }
  })))

logic synchronous_barriers__synchronous_barrier__release_threshold__first__bit1 : int

logic synchronous_barriers__synchronous_barrier__release_threshold__last__bit1 : int

logic synchronous_barriers__synchronous_barrier__release_threshold__position1 : int

axiom synchronous_barriers__synchronous_barrier__release_threshold__first__bit_axiom1 :
  (0 <= synchronous_barriers__synchronous_barrier__release_threshold__first__bit1)

axiom synchronous_barriers__synchronous_barrier__release_threshold__last__bit_axiom1 :
  (synchronous_barriers__synchronous_barrier__release_threshold__first__bit1 <  synchronous_barriers__synchronous_barrier__release_threshold__last__bit1)

axiom synchronous_barriers__synchronous_barrier__release_threshold__position_axiom1 :
  (0 <= synchronous_barriers__synchronous_barrier__release_threshold__position1)

logic user_eq2 : us_rep1, us_rep1 -> bool

type tTsbSP__ref = { tTsbSP__content : us_rep1
}

function tTsbSP__ref___projection(a: tTsbSP__ref) : us_rep1 =
  (a).tTsbSP__content

logic sb__split_discrs : barrier_limit

logic attr__ATTRIBUTE_ADDRESS : int

logic us : int

 (* File "system.ads", line 81, characters 0-0 *)
goal WP_parameter_def : in_range(1)

(set-info :smt-lib-version 2.6)
(set-logic QF_FP)
(set-info :source |Random FP created by PyMPF|)
(set-info :license |https://www.gnu.org/licenses/gpl-3.0.html|)
(set-info :category random)
(set-info :status unsat)
(declare-const x Float32)
(assert (= x (_ -oo 8 24)))
;; x should be Float32(-oo)

(declare-const y Float32)
(assert (= y ((_ to_fp 8 24) #xB4FE5040)))
;; y should be Float32(0xB4FE5040 [Rational(-260417, 549755813888), -0.000000])

(declare-const z Float32)
(assert (= z ((_ to_fp 8 24) #x4AD7404B)))
;; z should be Float32(0x4AD7404B [Rational(14106699, 2), 7053349.500000])

(declare-const result Float32)
(assert (= result (fp.fma RNA x y z)))
(assert (not (= result (_ +oo 8 24))))
(check-sat)
(exit)

(set-info :smt-lib-version 2.6)
(set-logic QF_FP)
(set-option :produce-models true)
(set-info :source |Random FP created by PyMPF|)
(set-info :license |https://www.gnu.org/licenses/gpl-3.0.html|)
(set-info :category random)
(set-info :status sat)
(declare-const x Float32)
(assert (= x (fp #b1 #b11111111 #b01100011110110011111010)))
;; x should be Float32(NaN)

(declare-const y Float32)
(assert (= y (fp #b0 #b10010101 #b01010101011001011110101)))
;; y should be Float32(0x4AAAB2F5 [Rational(11186933, 2), 5593466.500000])

(declare-const z Float32)
(assert (= z ((_ to_fp 8 24) #xCADCD3BB)))
;; z should be Float32(0xCADCD3BB [Rational(-14472123, 2), -7236061.500000])

(declare-const result Float32)
(assert (= result (fp.fma RTN x y z)))
(assert (= result (_ NaN 8 24)))
(check-sat)
(exit)

(set-info :smt-lib-version 2.6)
(set-logic QF_FP)
(set-info :source |Random FP created by PyMPF|)
(set-info :license |https://www.gnu.org/licenses/gpl-3.0.html|)
(set-info :category random)
(set-info :status unsat)
(declare-const x Float32)
(assert (= x ((_ to_fp 8 24) #xCAC9F767)))
;; x should be Float32(0xCAC9F767 [Rational(-13236071, 2), -6618035.500000])

(declare-const y Float32)
(assert (= y ((_ to_fp 8 24) #xA73F71AA)))
;; y should be Float32(0xA73F71AA [Rational(-6273237, 2361183241434822606848), -0.000000])

(declare-const z Float32)
(assert (= z ((_ to_fp 8 24) #x80000000)))
;; z should be Float32(-zero)

(declare-const result Float32)
(assert (= result (fp.fma RTN x y z)))
(assert (not (= result ((_ to_fp 8 24) #x32970942))))
(check-sat)
(exit)

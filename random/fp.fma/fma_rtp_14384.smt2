(set-info :smt-lib-version 2.6)
(set-logic QF_FP)
(set-info :source |Random FP created by PyMPF|)
(set-info :license |https://www.gnu.org/licenses/gpl-3.0.html|)
(set-info :category random)
(set-info :status unsat)
(declare-const x Float32)
(assert (= x ((_ to_fp 8 24) #x4A9CC84F)))
;; x should be Float32(0x4A9CC84F [Rational(10274895, 2), 5137447.500000])

(declare-const y Float32)
(assert (= y ((_ to_fp 8 24) #xC970E93B)))
;; y should be Float32(0xC970E93B [Rational(-15788347, 16), -986771.687500])

(declare-const z Float32)
(assert (= z (_ +zero 8 24)))
;; z should be Float32(+zero)

(declare-const result Float32)
(assert (= result (fp.fma RTP x y z)))
(assert (not (= result ((_ to_fp 8 24) #xD4938AA0))))
(check-sat)
(exit)

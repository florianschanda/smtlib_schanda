(set-info :smt-lib-version 2.6)
(set-logic QF_FP)
(set-option :produce-models true)
(set-info :source |Random FP created by PyMPF|)
(set-info :license |https://www.gnu.org/licenses/gpl-3.0.html|)
(set-info :category random)
(set-info :status sat)
(declare-const x Float32)
(assert (= x ((_ to_fp 8 24) #x4811D4A0)))
;; x should be Float32(0x4811D4A0 [Rational(298661, 2), 149330.500000])

(declare-const y Float32)
(assert (= y ((_ to_fp 8 24) #xCA9ABDF7)))
;; y should be Float32(0xCA9ABDF7 [Rational(-10141175, 2), -5070587.500000])

(declare-const z Float32)
(assert (= z (_ +oo 8 24)))
;; z should be Float32(+oo)

(declare-const result Float32)
(assert (= result (fp.fma RTZ x y z)))
(assert (= result ((_ to_fp 8 24) #x7F800000)))
(check-sat)
(exit)

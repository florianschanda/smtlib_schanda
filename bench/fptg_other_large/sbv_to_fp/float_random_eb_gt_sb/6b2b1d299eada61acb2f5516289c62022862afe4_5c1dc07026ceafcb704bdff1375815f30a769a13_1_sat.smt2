(set-info :smt-lib-version 2.6)
(set-logic QF_BVFP)
(set-info :source |Generated by FPTG and PyMPF|)
(set-info :license |https://www.gnu.org/licenses/gpl-3.0.html|)
(set-info :category "random")
(set-info :status sat)

; Format: FLOAT_RANDOM_EB_GT_SB
; Arg1: 747

; Redundancy: False
; Validation for: SBV_TO_FP
;   * PYMPF: FULL
;   * NATIVE_SSE: NONE
;   * NATIVE_X87: NONE
;   * MPFR: PARTIAL
;   * SOFTFLOAT: NONE

(declare-const rm RoundingMode)

(declare-const arg1 (_ BitVec 11))
(assert (not (bvule arg1 #b01011101010)))
(assert (not (bvule #b01011101100 arg1)))

(declare-const result (_ FloatingPoint 11 10))
(assert (= result ((_ to_fp 11 10) rm arg1)))

(declare-const expect (_ FloatingPoint 11 10))
(assert (fp.leq (fp #b0 #b01011010011 #b111000010)
                expect
                (fp #b0 #b10000001000 #b011101011)))
(assert (fp.leq (fp #b0 #b10000001000 #b011101011)
                expect
                (fp #b0 #b11111011100 #b011100010)))

(assert (= expect result))

(check-sat)
(exit)

(set-info :smt-lib-version 2.6)
(set-logic QF_BVFP)
(set-info :source |Generated by FPTG and PyMPF|)
(set-info :license |https://www.gnu.org/licenses/gpl-3.0.html|)
(set-info :category "random")
(set-info :status sat)

; Format: FLOAT_RANDOM_EB_GT_SB
; Arg1: 2

; Redundancy: False
; Validation for: SBV_TO_FP
;   * PYMPF: FULL
;   * NATIVE_SSE: NONE
;   * NATIVE_X87: NONE
;   * MPFR: PARTIAL
;   * SOFTFLOAT: NONE

(declare-const rm RoundingMode)

(declare-const arg1 (_ BitVec 3))
(assert (not (bvule arg1 #b001)))
(assert (not (bvule #b011 arg1)))

(declare-const result (_ FloatingPoint 13 8))
(assert (= result ((_ to_fp 13 8) rm arg1)))

(declare-const expect (_ FloatingPoint 13 8))
(assert (fp.leq (fp #b0 #b0110011110110 #b1001011)
                expect
                (fp #b0 #b1000000000000 #b0000000)))
(assert (fp.leq (fp #b0 #b1000000000000 #b0000000)
                expect
                (fp #b0 #b1010101010011 #b0110101)))

(assert (= expect result))

(check-sat)
(exit)

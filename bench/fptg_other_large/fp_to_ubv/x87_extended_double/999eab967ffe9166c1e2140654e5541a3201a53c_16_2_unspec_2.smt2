(set-info :smt-lib-version 2.6)
(set-logic QF_BVFP)
(set-info :source |Generated by FPTG and PyMPF|)
(set-info :license |https://www.gnu.org/licenses/gpl-3.0.html|)
(set-info :category "random")
(set-info :status sat)

; Format: X87_EXTENDED_DOUBLE
; Target: bv16
; Arg1: +NAN

; Redundancy: False
; Validation for: FP_TO_UBV
;   * PYMPF: FULL

(declare-const rm RoundingMode)

(declare-const arg (_ FloatingPoint 15 79))
(assert (fp.isNaN arg))

(declare-const result (_ BitVec 16))
(assert (= result ((_ fp.to_ubv 16) rm arg)))

(declare-const expect (_ BitVec 16))
(assert (not (bvule expect #b0010000010111010)))
(assert (not (bvule #b0010000010111100 expect)))

(assert (= expect result))
; this benchmark exploits unspecified behaviour

(check-sat)
(exit)

(set-info :smt-lib-version 2.6)
(set-logic QF_BVFP)
(set-info :source |Generated by FPTG and PyMPF|)
(set-info :license |https://www.gnu.org/licenses/gpl-3.0.html|)
(set-info :category "random")
(set-info :status sat)

; Format: FLOAT_RANDOM_EB_GT_SB
; Target: bv16
; Arg1: +RANDOM_GT_INT_BOUNDARY(3)

; Redundancy: False
; Validation for: FP_TO_UBV
;   * PYMPF: FULL

(declare-const rm RoundingMode)

(declare-const arg (_ FloatingPoint 12 2))
(assert (fp.leq (fp #b0 #b011101110010 #b1)
                arg
                (fp #b0 #b101111001101 #b1)))
(assert (fp.leq (fp #b0 #b101111001101 #b1)
                arg
                (fp #b0 #b110101010100 #b1)))

(declare-const result (_ BitVec 16))
(assert (= result ((_ fp.to_ubv 16) rm arg)))

(declare-const expect (_ BitVec 16))
(assert (= expect #b1111110000110100))

(assert (= expect result))
; this benchmark exploits unspecified behaviour

(check-sat)
(exit)

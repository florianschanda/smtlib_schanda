(set-info :smt-lib-version 2.6)
(set-logic QF_BVFP)
(set-info :source |Generated by FPTG and PyMPF|)
(set-info :license |https://www.gnu.org/licenses/gpl-3.0.html|)
(set-info :category "random")
(set-info :status sat)

; Format: FLOAT_RANDOM_EB_GT_SB
; Target: bv18
; Arg1: +RANDOM_GT_INT_BOUNDARY

; Redundancy: False
; Validation for: FP_TO_SBV
;   * PYMPF: FULL

(declare-const rm RoundingMode)

(declare-const arg (_ FloatingPoint 14 10))
(assert (fp.leq (fp #b1 #b00100110110011 #b000001001)
                arg
                (fp #b0 #b10010011001111 #b110110001)))
(assert (fp.leq (fp #b0 #b10010011001111 #b110110001)
                arg
                (fp #b0 #b11000001000001 #b101010011)))

(declare-const result (_ BitVec 18))
(assert (= result ((_ fp.to_sbv 18) rm arg)))

(declare-const expect (_ BitVec 18))
(assert (not (bvule expect #b010100011111010000)))
(assert (not (bvule #b010100011111010010 expect)))

(assert (not (= expect result)))
; this benchmark exploits unspecified behaviour

(check-sat)
(exit)

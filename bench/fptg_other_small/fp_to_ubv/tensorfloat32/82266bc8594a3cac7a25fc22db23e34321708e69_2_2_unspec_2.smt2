(set-info :smt-lib-version 2.6)
(set-logic QF_BVFP)
(set-info :source |Generated by FPTG and PyMPF|)
(set-info :license |https://www.gnu.org/licenses/gpl-3.0.html|)
(set-info :category "random")
(set-info :status sat)

; Format: TENSORFLOAT32
; Target: bv2
; Arg1: +RANDOM_GT_INT_BOUNDARY(3)

; Redundancy: False
; Validation for: FP_TO_UBV
;   * PYMPF: FULL

(declare-const rm RoundingMode)

(declare-const arg (_ FloatingPoint 8 11))
(assert (fp.leq (fp #b1 #b01010101 #b1011111111)
                arg
                (fp #b0 #b10011111 #b0100101000)))
(assert (fp.leq (fp #b0 #b10011111 #b0100101000)
                arg
                (fp #b0 #b11101010 #b1110110101)))
; arg = 5536481280.0

(declare-const result (_ BitVec 2))
(assert (= result ((_ fp.to_ubv 2) rm arg)))

(declare-const expect (_ BitVec 2))
(assert (not (bvule expect #b10)))

(assert (= expect result))
; this benchmark exploits unspecified behaviour

(check-sat)
(exit)

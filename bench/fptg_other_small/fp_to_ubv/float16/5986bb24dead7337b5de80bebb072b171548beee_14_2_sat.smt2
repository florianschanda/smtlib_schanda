(set-info :smt-lib-version 2.6)
(set-logic QF_BVFP)
(set-info :source |Generated by FPTG and PyMPF|)
(set-info :license |https://www.gnu.org/licenses/gpl-3.0.html|)
(set-info :category "random")
(set-info :status sat)

; Format: FLOAT16
; Target: bv14
; Arg1: +RANDOM_NORMAL_GE_TWO(4)

; Redundancy: False
; Validation for: FP_TO_UBV
;   * PYMPF: FULL
;   * NATIVE_SSE: NONE
;   * NATIVE_X87: NONE
;   * MPFR: NONE
;   * SOFTFLOAT: NONE

(declare-const rm RoundingMode)

(declare-const arg Float16)
(assert (fp.leq (fp #b0 #b10001 #b1001011000)
                arg
                (fp #b0 #b11100 #b1101000111)))
(assert (fp.leq (fp #b0 #b11100 #b1101000111)
                arg
                (fp #b0 #b11100 #b1101010101)))
; arg = 14904.0

(declare-const result (_ BitVec 14))
(assert (= result ((_ fp.to_ubv 14) rm arg)))

(declare-const expect (_ BitVec 14))
(assert (not (bvule expect #b11101000110111)))
(assert (not (bvule #b11101000111001 expect)))

(assert (= expect result))

(check-sat)
(exit)

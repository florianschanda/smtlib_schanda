(set-info :smt-lib-version 2.6)
(set-logic QF_BVFP)
(set-info :source |Generated by FPTG and PyMPF|)
(set-info :license |https://www.gnu.org/licenses/gpl-3.0.html|)
(set-info :category "random")
(set-info :status sat)

; Format: FLOAT_RANDOM_EB_GT_SB
; Target: bv62
; Arg1: +RCP_SMALLEST_SUBNORMAL

; Redundancy: False
; Validation for: FP_TO_UBV
;   * PYMPF: FULL

(declare-const rm RoundingMode)

(declare-const arg (_ FloatingPoint 7 2))
(assert (fp.leq (fp #b0 #b1110010 #b0)
                arg
                (fp #b0 #b1111110 #b0)))
(assert (fp.leq (fp #b0 #b1111110 #b0)
                arg
                (fp #b0 #b1111111 #b0)))
; arg = 9223372036854775808.0

(declare-const result (_ BitVec 62))
(assert (= result ((_ fp.to_ubv 62) rm arg)))

(declare-const expect (_ BitVec 62))
(assert (= expect #b00110111100111010011010111000110011110011001111000100000110101))

(assert (= expect result))
; this benchmark exploits unspecified behaviour

(check-sat)
(exit)

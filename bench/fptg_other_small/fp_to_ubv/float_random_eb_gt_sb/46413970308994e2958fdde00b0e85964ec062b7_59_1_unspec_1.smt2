(set-info :smt-lib-version 2.6)
(set-logic QF_BVFP)
(set-info :source |Generated by FPTG and PyMPF|)
(set-info :license |https://www.gnu.org/licenses/gpl-3.0.html|)
(set-info :category "random")
(set-info :status sat)

; Format: FLOAT_RANDOM_EB_GT_SB
; Target: bv59
; Arg1: -RANDOM_NORMAL_LT_TWO(2)

; Redundancy: False
; Validation for: FP_TO_UBV
;   * PYMPF: FULL

(declare-const rm RoundingMode)

(declare-const arg (_ FloatingPoint 4 2))
(assert (fp.leq (fp #b1 #b1011 #b0)
                arg
                (fp #b1 #b0111 #b1)))
(assert (fp.leq (fp #b1 #b0111 #b1)
                arg
                (fp #b0 #b0111 #b0)))
; arg = -1.5

(declare-const result (_ BitVec 59))
(assert (= result ((_ fp.to_ubv 59) rm arg)))

(declare-const expect (_ BitVec 59))
(assert (not (bvule expect #b00100100100110100101000000010000100010111001000111000011001)))
(assert (not (bvule #b00100100100110100101000000010000100010111001000111000011011 expect)))

(assert (not (= expect result)))
; this benchmark exploits unspecified behaviour

(check-sat)
(exit)

(set-info :smt-lib-version 2.6)
(set-logic QF_BVFP)
(set-info :source |Generated by FPTG and PyMPF|)
(set-info :license |https://www.gnu.org/licenses/gpl-3.0.html|)
(set-info :category "random")
(set-info :status sat)

; Format: FLOAT16
; Arg1: 1950

; Redundancy: False
; Validation for: UBV_TO_FP
;   * PYMPF: FULL
;   * NATIVE_SSE: NONE
;   * NATIVE_X87: NONE
;   * MPFR: PARTIAL
;   * SOFTFLOAT: NONE

(declare-const rm RoundingMode)

(declare-const arg1 (_ BitVec 11))
(assert (not (bvule arg1 #b11110011101)))
(assert (not (bvule #b11110011111 arg1)))

(declare-const result Float16)
(assert (= result ((_ to_fp_unsigned 5 11) rm arg1)))

(declare-const expect Float16)
(assert (fp.leq (fp #b1 #b01110 #b0011011101)
                expect
                (fp #b0 #b11001 #b1110011110)))
(assert (fp.leq (fp #b0 #b11001 #b1110011110)
                expect
                (fp #b0 #b11101 #b1110100010)))
; expect = 1950.0

(assert (= expect result))

(check-sat)
(exit)

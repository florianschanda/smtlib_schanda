(set-info :smt-lib-version 2.6)
(set-logic QF_BVFP)
(set-info :source |Generated by FPTG and PyMPF|)
(set-info :license |https://www.gnu.org/licenses/gpl-3.0.html|)
(set-info :category "random")
(set-info :status sat)

; Format: FLOAT16
; Arg1: 2046

; Redundancy: False
; Validation for: UBV_TO_FP
;   * PYMPF: FULL
;   * NATIVE_SSE: NONE
;   * NATIVE_X87: NONE
;   * MPFR: PARTIAL
;   * SOFTFLOAT: NONE

(declare-const rm RoundingMode)

(declare-const arg1 (_ BitVec 11))
(assert (not (bvule arg1 #b11111111101)))
(assert (not (bvule #b11111111111 arg1)))

(declare-const result Float16)
(assert (= result ((_ to_fp_unsigned 5 11) rm arg1)))

(declare-const expect Float16)
(assert (fp.leq (fp #b1 #b11101 #b1110111100)
                expect
                (fp #b0 #b11001 #b1111111110)))
(assert (fp.leq (fp #b0 #b11001 #b1111111110)
                expect
                (fp #b0 #b11010 #b0011110101)))
; expect = 2046.0

(assert (= expect result))

(check-sat)
(exit)

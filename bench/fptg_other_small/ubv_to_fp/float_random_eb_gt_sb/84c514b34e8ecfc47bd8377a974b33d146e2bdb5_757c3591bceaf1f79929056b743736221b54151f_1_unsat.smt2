(set-info :smt-lib-version 2.6)
(set-logic QF_BVFP)
(set-info :source |Generated by FPTG and PyMPF|)
(set-info :license |https://www.gnu.org/licenses/gpl-3.0.html|)
(set-info :category "random")
(set-info :status unsat)

; Format: FLOAT_RANDOM_EB_GT_SB
; Arg1: 32767

; Redundancy: False
; Validation for: UBV_TO_FP
;   * PYMPF: FULL
;   * NATIVE_SSE: NONE
;   * NATIVE_X87: NONE
;   * MPFR: PARTIAL
;   * SOFTFLOAT: NONE

(declare-const rm RoundingMode)
(assert (or (= rm RTN) (= rm RTZ)))

(declare-const arg1 (_ BitVec 15))
(assert (not (bvule arg1 #b111111111111110)))

(declare-const result (_ FloatingPoint 6 5))
(assert (= result ((_ to_fp_unsigned 6 5) rm arg1)))

(declare-const expect (_ FloatingPoint 6 5))
(assert (fp.leq (fp #b0 #b010101 #b0011)
                expect
                (fp #b0 #b101101 #b1111)))
(assert (fp.leq (fp #b0 #b101101 #b1111)
                expect
                (fp #b0 #b111001 #b1001)))
; expect = 31744.0

(assert (not (= expect result)))

(check-sat)
(exit)

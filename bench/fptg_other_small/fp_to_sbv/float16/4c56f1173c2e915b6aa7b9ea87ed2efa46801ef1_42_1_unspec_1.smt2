(set-info :smt-lib-version 2.6)
(set-logic QF_BVFP)
(set-info :source |Generated by FPTG and PyMPF|)
(set-info :license |https://www.gnu.org/licenses/gpl-3.0.html|)
(set-info :category "random")
(set-info :status sat)

; Format: FLOAT16
; Target: bv42
; Arg1: +NAN

; Redundancy: False
; Validation for: FP_TO_SBV
;   * PYMPF: FULL

(declare-const rm RoundingMode)

(declare-const arg Float16)
(assert (fp.isNaN arg))
; arg = NaN

(declare-const result (_ BitVec 42))
(assert (= result ((_ fp.to_sbv 42) rm arg)))

(declare-const expect (_ BitVec 42))
(assert (not (bvule expect #b000101110001101110101011100100000011011000)))
(assert (not (bvule #b000101110001101110101011100100000011011010 expect)))

(assert (not (= expect result)))
; this benchmark exploits unspecified behaviour

(check-sat)
(exit)

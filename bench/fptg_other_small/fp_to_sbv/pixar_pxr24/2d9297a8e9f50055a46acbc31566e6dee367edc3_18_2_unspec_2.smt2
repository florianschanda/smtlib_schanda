(set-info :smt-lib-version 2.6)
(set-logic QF_BVFP)
(set-info :source |Generated by FPTG and PyMPF|)
(set-info :license |https://www.gnu.org/licenses/gpl-3.0.html|)
(set-info :category "random")
(set-info :status sat)

; Format: PIXAR_PXR24
; Target: bv18
; Arg1: +NAN

; Redundancy: False
; Validation for: FP_TO_SBV
;   * PYMPF: FULL

(declare-const rm RoundingMode)

(declare-const arg (_ FloatingPoint 8 16))
(assert (fp.isNaN arg))
; arg = NaN

(declare-const result (_ BitVec 18))
(assert (= result ((_ fp.to_sbv 18) rm arg)))

(declare-const expect (_ BitVec 18))
(assert (not (bvule expect #b101101000000100011)))
(assert (not (bvule #b101101000000100101 expect)))

(assert (= expect result))
; this benchmark exploits unspecified behaviour

(check-sat)
(exit)

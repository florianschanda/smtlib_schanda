(set-info :smt-lib-version 2.6)
(set-logic QF_BVFP)
(set-info :source |Generated by FPTG and PyMPF|)
(set-info :license |https://www.gnu.org/licenses/gpl-3.0.html|)
(set-info :category "random")
(set-info :status sat)

; Format: TENSORFLOAT32
; Target: bv48
; Arg1: -NAN

; Redundancy: False
; Validation for: FP_TO_SBV
;   * PYMPF: FULL

(declare-const rm RoundingMode)

(declare-const arg (_ FloatingPoint 8 11))
(assert (fp.isNaN arg))
; arg = NaN

(declare-const result (_ BitVec 48))
(assert (= result ((_ fp.to_sbv 48) rm arg)))

(declare-const expect (_ BitVec 48))
(assert (not (bvule expect #b110110001101010011001000101000010001110111000111)))
(assert (not (bvule #b110110001101010011001000101000010001110111001001 expect)))

(assert (not (= expect result)))
; this benchmark exploits unspecified behaviour

(check-sat)
(exit)

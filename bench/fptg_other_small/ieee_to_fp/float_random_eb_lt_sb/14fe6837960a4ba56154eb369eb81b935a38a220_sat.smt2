(set-info :smt-lib-version 2.6)
(set-logic QF_BVFP)
(set-info :source |Generated by FPTG and PyMPF|)
(set-info :license |https://www.gnu.org/licenses/gpl-3.0.html|)
(set-info :category "random")
(set-info :status sat)

; Format: FLOAT_RANDOM_EB_LT_SB
; Arg1: -NAN

(declare-const arg1 (_ BitVec 15))
(assert (not (bvule arg1 #b111111100110011)))
(assert (not (bvule #b111111100110101 arg1)))

(declare-const result (_ FloatingPoint 6 9))
(assert (= result ((_ to_fp 6 9) arg1)))

(declare-const expect (_ FloatingPoint 6 9))
(assert (fp.isNaN expect))
; expect = NaN

(assert (= expect result))

(check-sat)
(exit)

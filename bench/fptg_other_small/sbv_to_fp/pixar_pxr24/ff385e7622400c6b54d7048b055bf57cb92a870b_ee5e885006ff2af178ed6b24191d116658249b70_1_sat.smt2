(set-info :smt-lib-version 2.6)
(set-logic QF_BVFP)
(set-info :source |Generated by FPTG and PyMPF|)
(set-info :license |https://www.gnu.org/licenses/gpl-3.0.html|)
(set-info :category "random")
(set-info :status sat)

; Format: PIXAR_PXR24
; Arg1: -3226

; Redundancy: False
; Validation for: SBV_TO_FP
;   * PYMPF: FULL
;   * NATIVE_SSE: NONE
;   * NATIVE_X87: NONE
;   * MPFR: PARTIAL
;   * SOFTFLOAT: NONE

(declare-const rm RoundingMode)

(declare-const arg1 (_ BitVec 13))
(assert (not (bvule arg1 #b1001101100101)))
(assert (not (bvule #b1001101100111 arg1)))

(declare-const result (_ FloatingPoint 8 16))
(assert (= result ((_ to_fp 8 16) rm arg1)))

(declare-const expect (_ FloatingPoint 8 16))
(assert (fp.leq (fp #b1 #b11110011 #b100000000100010)
                expect
                (fp #b1 #b10001010 #b100100110100000)))
(assert (fp.leq (fp #b1 #b10001010 #b100100110100000)
                expect
                (fp #b0 #b10100111 #b010111101101100)))
; expect = -3226.0

(assert (= expect result))

(check-sat)
(exit)

(set-info :smt-lib-version 2.6)
(set-logic QF_BVFP)
(set-info :source |Generated by FPTG and PyMPF|)
(set-info :license |https://www.gnu.org/licenses/gpl-3.0.html|)
(set-info :category "random")
(set-info :status unsat)

; Format: FLOAT16
; Arg1: -54190

; Redundancy: False
; Validation for: SBV_TO_FP
;   * PYMPF: FULL
;   * NATIVE_SSE: NONE
;   * NATIVE_X87: NONE
;   * MPFR: PARTIAL
;   * SOFTFLOAT: NONE

(declare-const rm RoundingMode)
(assert (not (= rm RTN)))

(declare-const arg1 (_ BitVec 17))
(assert (not (bvule arg1 #b10010110001010001)))
(assert (not (bvule #b10010110001010011 arg1)))

(declare-const result Float16)
(assert (= result ((_ to_fp 5 11) rm arg1)))

(declare-const expect Float16)
(assert (fp.leq (fp #b1 #b11110 #b1011110001)
                expect
                (fp #b1 #b11110 #b1010011101)))
(assert (fp.leq (fp #b1 #b11110 #b1010011101)
                expect
                (fp #b0 #b10010 #b0101100100)))
; expect = -54176.0

(assert (not (= expect result)))

(check-sat)
(exit)

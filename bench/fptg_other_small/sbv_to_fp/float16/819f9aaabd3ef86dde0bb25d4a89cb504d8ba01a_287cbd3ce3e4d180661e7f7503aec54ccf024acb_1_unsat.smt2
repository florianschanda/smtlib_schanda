(set-info :smt-lib-version 2.6)
(set-logic QF_BVFP)
(set-info :source |Generated by FPTG and PyMPF|)
(set-info :license |https://www.gnu.org/licenses/gpl-3.0.html|)
(set-info :category "random")
(set-info :status unsat)

; Format: FLOAT16
; Arg1: -51623

; Redundancy: False
; Validation for: SBV_TO_FP
;   * PYMPF: FULL
;   * NATIVE_SSE: NONE
;   * NATIVE_X87: NONE
;   * MPFR: PARTIAL
;   * SOFTFLOAT: NONE

(declare-const rm RoundingMode)
(assert (not (= rm RTN)))

(declare-const arg1 (_ BitVec 17))
(assert (not (bvule arg1 #b10011011001011000)))
(assert (not (bvule #b10011011001011010 arg1)))

(declare-const result Float16)
(assert (= result ((_ to_fp 5 11) rm arg1)))

(declare-const expect Float16)
(assert (fp.leq (fp #b1 #b11110 #b1010010000)
                expect
                (fp #b1 #b11110 #b1001001101)))
(assert (fp.leq (fp #b1 #b11110 #b1001001101)
                expect
                (fp #b1 #b11011 #b1100111101)))
; expect = -51616.0

(assert (not (= expect result)))

(check-sat)
(exit)

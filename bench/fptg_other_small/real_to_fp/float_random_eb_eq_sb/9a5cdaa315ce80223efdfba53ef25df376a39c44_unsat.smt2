(set-info :smt-lib-version 2.6)
(set-logic QF_FPLRA)
(set-info :source |Generated by FPTG and PyMPF|)
(set-info :license |https://www.gnu.org/licenses/gpl-3.0.html|)
(set-info :category "random")
(set-info :status unsat)

; Format: FLOAT_RANDOM_EB_EQ_SB
; Target class: -RANDOM_NORMAL_LT_ONE(5)

; Redundancy: False
; Validation for: REAL_TO_FP
;   * PYMPF: FULL
;   * NATIVE_SSE: NONE
;   * NATIVE_X87: NONE
;   * MPFR: PARTIAL
;   * SOFTFLOAT: NONE

(declare-const rm RoundingMode)

(declare-const arg1 Real)
(assert (<= arg1 (- (/ 21.0 262144.0))))
(assert (<= (- (/ 21.0 262144.0)) arg1))

(declare-const result (_ FloatingPoint 5 5))
(assert (= result ((_ to_fp 5 5) rm arg1)))

(declare-const expect (_ FloatingPoint 5 5))
(assert (fp.leq (fp #b1 #b01111 #b0110)
                expect
                (fp #b1 #b00001 #b0101)))
(assert (fp.leq (fp #b1 #b00001 #b0101)
                expect
                (fp #b0 #b01010 #b0111)))
; expect = -0.000080108642578125

(assert (not (= expect result)))

(check-sat)
(exit)

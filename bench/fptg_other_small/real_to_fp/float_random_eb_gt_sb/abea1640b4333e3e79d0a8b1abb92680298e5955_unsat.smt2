(set-info :smt-lib-version 2.6)
(set-logic QF_FPLRA)
(set-info :source |Generated by FPTG and PyMPF|)
(set-info :license |https://www.gnu.org/licenses/gpl-3.0.html|)
(set-info :category "random")
(set-info :status unsat)

; Format: FLOAT_RANDOM_EB_GT_SB
; Target class: +LARGEST_NORMAL

; Redundancy: False
; Validation for: REAL_TO_FP
;   * PYMPF: FULL
;   * NATIVE_SSE: NONE
;   * NATIVE_X87: NONE
;   * MPFR: PARTIAL
;   * SOFTFLOAT: NONE

(declare-const rm RoundingMode)

(declare-const arg1 Real)
(assert (<= arg1 13835058055282163712.0))
(assert (<= 13835058055282163712.0 arg1))

(declare-const result (_ FloatingPoint 7 2))
(assert (= result ((_ to_fp 7 2) rm arg1)))

(declare-const expect (_ FloatingPoint 7 2))
(assert (fp.leq (fp #b1 #b0011001 #b0)
                expect
                (fp #b0 #b1111110 #b1)))
(assert (fp.leq (fp #b0 #b1111110 #b1)
                expect
                (fp #b0 #b1111111 #b0)))
; expect = 13835058055282163712.0

(assert (not (= expect result)))

(check-sat)
(exit)

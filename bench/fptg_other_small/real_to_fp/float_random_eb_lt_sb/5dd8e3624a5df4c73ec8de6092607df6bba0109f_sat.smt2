(set-info :smt-lib-version 2.6)
(set-logic QF_FPLRA)
(set-info :source |Generated by FPTG and PyMPF|)
(set-info :license |https://www.gnu.org/licenses/gpl-3.0.html|)
(set-info :category "random")
(set-info :status sat)

; Format: FLOAT_RANDOM_EB_LT_SB
; Target class: -RANDOM_SUBNORMAL(4)

; Redundancy: False
; Validation for: REAL_TO_FP
;   * PYMPF: FULL
;   * NATIVE_SSE: NONE
;   * NATIVE_X87: NONE
;   * MPFR: PARTIAL
;   * SOFTFLOAT: NONE

(declare-const rm RoundingMode)

(declare-const arg1 Real)
(assert (<= arg1 (- (/ 47.0 64.0))))
(assert (<= (- (/ 47.0 64.0)) arg1))

(declare-const result (_ FloatingPoint 2 9))
(assert (= result ((_ to_fp 2 9) rm arg1)))

(declare-const expect (_ FloatingPoint 2 9))
(assert (fp.leq (fp #b1 #b01 #b11111110)
                expect
                (fp #b1 #b00 #b10111100)))
(assert (fp.leq (fp #b1 #b00 #b10111100)
                expect
                (fp #b1 #b00 #b01011100)))
; expect = -0.734375

(assert (= expect result))

(check-sat)
(exit)

(set-info :smt-lib-version 2.6)
(set-logic QF_FP)
(set-option :produce-models true)
(set-info :source |Random FP created by PyMPF|)
(set-info :license |https://www.gnu.org/licenses/gpl-3.0.html|)
(set-info :category random)
(set-info :status sat)
(declare-const x Float32)
(assert (= x ((_ to_fp 8 24) #x3720BD06)))
;; x should be Float32(0x3720BD06 [Rational(5267075, 549755813888), 0.000010])

(declare-const y Float32)
(assert (= y ((_ to_fp 8 24) #xCA7BA78A)))
;; y should be Float32(0xCA7BA78A [Rational(-8246213, 2), -4123106.500000])

(declare-const z Float32)
(assert (= z ((_ to_fp 8 24) #x7F8C3B82)))
;; z should be Float32(NaN)

(declare-const result Float32)
(assert (= result (fp.fma RTN x y z)))
(assert (= result (_ NaN 8 24)))
(check-sat)
(exit)

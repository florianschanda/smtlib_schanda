(set-info :smt-lib-version 2.6)
(set-logic QF_FP)
(set-option :produce-models true)
(set-info :source |Random FP created by PyMPF|)
(set-info :license |https://www.gnu.org/licenses/gpl-3.0.html|)
(set-info :category random)
(set-info :status sat)
(declare-const x Float32)
(assert (= x (_ +zero 8 24)))
;; x should be Float32(+zero)

(declare-const y Float32)
(assert (= y ((_ to_fp 8 24) #xC9DE01BC)))
;; y should be Float32(0xC9DE01BC [Rational(-3637359, 2), -1818679.500000])

(declare-const z Float32)
(assert (= z ((_ to_fp 8 24) #xCA7C6102)))
;; z should be Float32(0xCA7C6102 [Rational(-8269953, 2), -4134976.500000])

(declare-const result Float32)
(assert (= result (fp.fma RTN x y z)))
(assert (= result ((_ to_fp 8 24) #xCA7C6102)))
(check-sat)
(exit)

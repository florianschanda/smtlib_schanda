(set-info :smt-lib-version 2.6)
(set-logic QF_FP)
(set-info :source |Random FP created by PyMPF|)
(set-info :license |https://www.gnu.org/licenses/gpl-3.0.html|)
(set-info :category random)
(set-info :status unsat)
(declare-const x Float32)
(assert (= x ((_ to_fp 8 24) #xCAFD9697)))
;; x should be Float32(0xCAFD9697 [Rational(-16619159, 2), -8309579.500000])

(declare-const y Float32)
(assert (= y ((_ to_fp 8 24) #xC9665807)))
;; y should be Float32(0xC9665807 [Rational(-15095815, 16), -943488.437500])

(declare-const z Float32)
(assert (= z (_ -oo 8 24)))
;; z should be Float32(-oo)

(declare-const result Float32)
(assert (= result (fp.fma RTN x y z)))
(assert (not (= result (_ -oo 8 24))))
(check-sat)
(exit)

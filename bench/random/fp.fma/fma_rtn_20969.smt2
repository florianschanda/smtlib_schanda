(set-info :smt-lib-version 2.6)
(set-logic QF_FP)
(set-option :produce-models true)
(set-info :source |Random FP created by PyMPF|)
(set-info :license |https://www.gnu.org/licenses/gpl-3.0.html|)
(set-info :category random)
(set-info :status sat)
(declare-const x Float32)
(assert (= x ((_ to_fp 8 24) #x499698A4)))
;; x should be Float32(0x499698A4 [Rational(2467369, 2), 1233684.500000])

(declare-const y Float32)
(assert (= y ((_ to_fp 8 24) #x4AB6F11B)))
;; y should be Float32(0x4AB6F11B [Rational(11989275, 2), 5994637.500000])

(declare-const z Float32)
(assert (= z (_ -oo 8 24)))
;; z should be Float32(-oo)

(declare-const result Float32)
(assert (= result (fp.fma RTN x y z)))
(assert (= result ((_ to_fp 8 24) #xFF800000)))
(check-sat)
(exit)
